# CF-B-tree
CF‑B‑tree: An order‑preserving B‑tree index for arbitrary‑precision real numbers using continued fractions. CFKeys provide lexicographically sortable binary encodings whose byte order matches real‑number order exactly. Fast insertion, lookup, and range queries with no big‑integer arithmetic.
